{% extends 'base.html.twig' %}

{% block title %}Liste des acteurs{% endblock %}

{% block body %}
    <h1>Liste de tous les acteurs </h1>
    <ul>
     {% for actors in actor %}
     <li><a href="{{ path("actor_show", { slug: actors.slug }) }}">{{ actors.firstname }}</a></li>
    {% else %}
    <li>Aucun acteur pour cette série</li>
    {% endfor %}
{% endblock %}