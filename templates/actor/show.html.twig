{% extends 'base.html.twig' %}

{% block title %}{{ actors.firstname }}{% endblock %}

{% block body %}
    <h1>{{ actors.firstname }}</h1>
    <div>
        <strong>Participe aux séries :</strong>
        <ul>
            {% for program in actors.programs %}
                <li><a href="{{ path('program_show', {id: program.id}) }}">{{ program.title }}</a></li>
            {% else %}
                <li>Aucune série associée</li>
            {% endfor %}
        </ul>
    </div>

    <a href="{{ path('actor_index') }}" class="btn btn-secondary">
        Retour à la liste des acteurs
    </a>
{% endblock %}